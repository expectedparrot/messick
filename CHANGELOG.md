# Changelog

## Unreleased

- Made `agent next` a portable, versioned workflow contract through relevant analyses, report handoffs, and strict validation.
- Added explicit non-empty AgentList/ModelList execution designs, an ep-agent-selected required ModelList, bounded respondent pilot defaults, deterministic expected-call counts, and provenance hashes.

## 0.2.0 - 2026-08-10

- Replaced the 0.1 prototype with an agent-first project and evidence model.
- Added strict JSON-envelope CLI, immutable Survey imports, project events,
  intents, scales, issues, agent control commands, doctor, and report context.
- Removed legacy `.json.gz` result loading and permissive option forwarding.
- Added deterministic wording, option, branching, burden, and scoring checks.
- Added real git-backed EDSL Jobs generation plus Survey/Results package loading.
- Added simulated and human evidence provenance, cognitive normalization,
  response diagnostics, classical scale diagnostics, source comparison, and
  claim-specific validation statuses.
- Added static documentation, schemas, a simulation-only example, CI, locked
  dependencies, and the Messick survey-parrot artwork.
